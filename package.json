{
  "name": "idrn-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "IDRN — public landing page + command-center portal, served as one unit.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
